# Copy this file to .env and fill in a value to enable the optional
# AI-assisted narrative. .env is git-ignored - never commit real keys.
#
# Not used by the core assignment (the competition assessment is fully
# rule-based). Reserved for the optional extension.

# Google AI Studio key - https://aistudio.google.com/apikey (free tier)
GEMINI_API_KEY=
